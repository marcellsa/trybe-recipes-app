import React from 'react';
import { render, screen } from '@testing-library/react';
import App from '../App';

// importar páginas

describe('Testando o compoenente Footer', () => {
  test('Se o botão de meals está presente na página Meals', () => {

  })

  test('Se o botão de drinks está presente na página Meals', () => {

  })
})